{
  "buildCommand": "npm install && npm run build -w @hide-and-seek/shared && VITE_PEER_MULTIPLAYER=true VITE_WS_URL= npm run build -w @hide-and-seek/client",
  "outputDirectory": "packages/client/dist",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
